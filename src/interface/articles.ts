export interface Task {
  title: string;
  contant: string;
  auther: string;
  email: string;
}

export interface Rating {
  rating: number;
}

export interface ModifiedTask {
  id: number;
  title: string;
  contant: string;
  auther: string;
  email: string;
}
